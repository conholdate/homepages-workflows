name: Deploy Homepage
run-name: "Deploy ${{ inputs.site }} → ${{ inputs.environment }} (${{ inputs.ref }})${{ inputs.transaction_id && format(' tx={0}', inputs.transaction_id) || '' }}${{ inputs.reconcile_identity && ' [identity-only]' || (!inputs.deploy && ' [build-only]' || '') }}"

on:
  workflow_dispatch:
    inputs:
      site:
        description: Homepage site to build or deploy
        required: true
        type: string
      environment:
        description: Target environment
        required: true
        type: choice
        options:
          - qa
          - production
      ref:
        description: Branch, tag, or SHA in conholdate/homepages
        required: true
        default: main
      deploy:
        description: Deploy after build
        required: true
        type: boolean
        default: false
      invalidate_cache:
        description: Invalidate CloudFront or BunnyCDN after deploy
        required: true
        type: boolean
        default: false
      transaction_id:
        description: Optional production transaction id used by Homepages Agent to correlate and poll workflow runs
        required: false
        type: string
        default: ""
      reconcile_identity:
        description: Republish a verified identity artifact without rebuilding or deploying homepage content
        required: true
        type: boolean
        default: false
      identity_run_id:
        description: Original Deploy Homepage run containing the exact identity artifact
        required: false
        type: string
        default: ""
      identity_run_attempt:
        description: Original Deploy Homepage run attempt containing the exact identity artifact
        required: false
        type: string
        default: ""

permissions:
  actions: read
  contents: read

concurrency:
  group: homepage-${{ inputs.site }}-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  build-deploy:
    name: ${{ inputs.site }} / ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    defaults:
      run:
        working-directory: homepages-source

    steps:
      - name: Checkout workflow support
        uses: actions/checkout@v4
        with:
          path: workflow-support

      - name: Checkout homepages source
        uses: actions/checkout@v4
        with:
          repository: conholdate/homepages
          ref: ${{ inputs.ref }}
          token: ${{ secrets.HOMEPAGES_SOURCE_PAT }}
          submodules: recursive
          fetch-depth: 0
          path: homepages-source

      - name: Resolve checked-out homepages source
        id: source
        shell: bash
        run: |
          set -euo pipefail
          source_sha="$(git rev-parse HEAD)"
          git cat-file -e "${source_sha}^{commit}"
          echo "source_sha=${source_sha}" >> "$GITHUB_OUTPUT"
          echo "Resolved conholdate/homepages source: ${source_sha}" >> "$GITHUB_STEP_SUMMARY"

      - name: Validate identity reconciliation inputs
        if: ${{ inputs.reconcile_identity }}
        shell: bash
        env:
          IDENTITY_RUN_ID: ${{ inputs.identity_run_id }}
          IDENTITY_RUN_ATTEMPT: ${{ inputs.identity_run_attempt }}
        run: |
          set -euo pipefail
          if [ "${{ inputs.deploy }}" != "true" ]; then
            echo "::error::Identity reconciliation requires deploy=true."
            exit 1
          fi
          if ! [[ "$IDENTITY_RUN_ID" =~ ^[1-9][0-9]*$ ]] || ! [[ "$IDENTITY_RUN_ATTEMPT" =~ ^[1-9][0-9]*$ ]]; then
            echo "::error::Identity reconciliation requires positive original run and attempt IDs."
            exit 1
          fi

      - name: Resolve registered deployment profile
        id: map
        shell: bash
        run: |
          set -euo pipefail
          python ../workflow-support/.github/scripts/resolve_deployment_profile.py \
            --manifest docs/homepage-sites-manifest.yaml \
            --profiles ../workflow-support/.github/deployment-profiles.json \
            --config-root . \
            --site "${{ inputs.site }}" \
            --environment "${{ inputs.environment }}" >> "$GITHUB_OUTPUT"

      - name: Setup Hugo
        if: ${{ !inputs.reconcile_identity }}
        env:
          HUGO_VERSION: ${{ steps.map.outputs.hugo_version }}
        run: |
          set -euo pipefail
          release_root="https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}"
          if ! curl -fsSL -o /tmp/hugo.tar.gz "${release_root}/hugo_extended_withdeploy_${HUGO_VERSION}_linux-amd64.tar.gz"; then
            curl -fsSL -o /tmp/hugo.tar.gz "${release_root}/hugo_extended_${HUGO_VERSION}_Linux-64bit.tar.gz"
          fi
          tar -xzf /tmp/hugo.tar.gz -C /tmp hugo
          sudo mv /tmp/hugo /usr/local/bin/hugo
          hugo version

      - name: Build
        if: ${{ !inputs.reconcile_identity }}
        run: hugo --config "${{ steps.map.outputs.config }}" --minify

      - name: Write deployment identity
        if: ${{ !inputs.reconcile_identity }}
        shell: bash
        run: |
          set -euo pipefail
          python ../workflow-support/.github/scripts/write_deployment_identity.py \
            --source-sha "${{ steps.source.outputs.source_sha }}" \
            --site "${{ inputs.site }}" \
            --environment "${{ inputs.environment }}" \
            --run-id "${{ github.run_id }}" \
            --run-attempt "${{ github.run_attempt }}" \
            --output-root public \
            --evidence-dir deployment-evidence

      - name: Upload deployment identity evidence
        if: ${{ !inputs.reconcile_identity }}
        uses: actions/upload-artifact@v4
        with:
          name: homepages-deployment-identity-${{ inputs.site }}-${{ inputs.environment }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: homepages-source/deployment-evidence/homepages-deployment.json
          if-no-files-found: error
          retention-days: 90

      - name: Download original deployment identity evidence
        if: ${{ inputs.reconcile_identity }}
        uses: actions/download-artifact@v4
        with:
          name: homepages-deployment-identity-${{ inputs.site }}-${{ inputs.environment }}-${{ inputs.identity_run_id }}-${{ inputs.identity_run_attempt }}
          path: homepages-source/deployment-evidence/reconcile
          github-token: ${{ github.token }}
          repository: conholdate/homepages-workflows
          run-id: ${{ inputs.identity_run_id }}

      - name: Verify original deployment identity evidence
        if: ${{ inputs.reconcile_identity }}
        shell: bash
        run: |
          set -euo pipefail
          python ../workflow-support/.github/scripts/verify_deployment_identity.py \
            --identity deployment-evidence/reconcile/homepages-deployment.json \
            --source-sha "${{ steps.source.outputs.source_sha }}" \
            --site "${{ inputs.site }}" \
            --environment "${{ inputs.environment }}" \
            --run-id "${{ inputs.identity_run_id }}" \
            --run-attempt "${{ inputs.identity_run_attempt }}"

      - name: Deploy
        if: ${{ inputs.deploy }}
        shell: bash
        env:
          CREDENTIAL_SET: ${{ steps.map.outputs.credential_set }}
          TARGETS: ${{ steps.map.outputs.targets }}
          ACCESS_KEY_SL: ${{ secrets.ACCESS_KEY_SL }}
          SECRET_ACCESS_SL: ${{ secrets.SECRET_ACCESS_SL }}
          ACCESS_KEY: ${{ secrets.ACCESS_KEY }}
          SECRET_ACCESS: ${{ secrets.SECRET_ACCESS }}
          HOMEPAGES_ACCESS_KEY: ${{ secrets.HOMEPAGES_ACCESS_KEY }}
          HOMEPAGES_SECRET_ACCESS: ${{ secrets.HOMEPAGES_SECRET_ACCESS }}
          CEPH_QA_ACCESS_KEY_ID: ${{ secrets.CEPH_QA_ACCESS_KEY_ID }}
          CEPH_QA_SECRET_ACCESS_KEY: ${{ secrets.CEPH_QA_SECRET_ACCESS_KEY }}
          CEPH_PRODUCTION_ACCESS_KEY_ID: ${{ secrets.CEPH_PRODUCTION_ACCESS_KEY_ID }}
          CEPH_PRODUCTION_SECRET_ACCESS_KEY: ${{ secrets.CEPH_PRODUCTION_SECRET_ACCESS_KEY }}
          CACHE_KIND: ${{ steps.map.outputs.cache_kind }}
          CACHE_ALIAS: ${{ steps.map.outputs.cache_alias }}
          INVALIDATE_CACHE: ${{ inputs.invalidate_cache }}
          AWS_DEFAULT_REGION: us-east-1
        run: |
          set -euo pipefail

          case "$CREDENTIAL_SET" in
            sl)
              export AWS_ACCESS_KEY_ID="$ACCESS_KEY_SL"
              export AWS_SECRET_ACCESS_KEY="$SECRET_ACCESS_SL"
              ;;
            standard)
              export AWS_ACCESS_KEY_ID="$ACCESS_KEY"
              export AWS_SECRET_ACCESS_KEY="$SECRET_ACCESS"
              ;;
            homepages)
              export AWS_ACCESS_KEY_ID="$HOMEPAGES_ACCESS_KEY"
              export AWS_SECRET_ACCESS_KEY="$HOMEPAGES_SECRET_ACCESS"
              ;;
            ceph_qa)
              export AWS_ACCESS_KEY_ID="$CEPH_QA_ACCESS_KEY_ID"
              export AWS_SECRET_ACCESS_KEY="$CEPH_QA_SECRET_ACCESS_KEY"
              export AWS_REQUEST_CHECKSUM_CALCULATION="when_required"
              ;;
            ceph_production)
              export AWS_ACCESS_KEY_ID="$CEPH_PRODUCTION_ACCESS_KEY_ID"
              export AWS_SECRET_ACCESS_KEY="$CEPH_PRODUCTION_SECRET_ACCESS_KEY"
              export AWS_REQUEST_CHECKSUM_CALCULATION="when_required"
              ;;
            *)
              echo "::error::Unsupported credential set."
              exit 1
              ;;
          esac

          if [ -z "${AWS_ACCESS_KEY_ID:-}" ] || [ -z "${AWS_SECRET_ACCESS_KEY:-}" ]; then
            echo "::error::Missing deploy credentials for credential set: $CREDENTIAL_SET"
            exit 1
          fi

          IFS=',' read -ra target_array <<< "$TARGETS"
          for target in "${target_array[@]}"; do
            identity_path="public/.well-known/homepages-deployment.json"
            if [ "${{ inputs.reconcile_identity }}" = "true" ]; then
              identity_path="deployment-evidence/reconcile/homepages-deployment.json"
            else
              hugo --config "${{ steps.map.outputs.config }}" deploy --target "$target" --maxDeletes -1 --force
            fi
            python ../workflow-support/.github/scripts/publish_deployment_identity.py \
              --config "${{ steps.map.outputs.config }}" \
              --target "$target" \
              --identity "$identity_path"
          done

          if [ "$INVALIDATE_CACHE" = "true" ] && [ "$CACHE_KIND" = "cloudfront-deploy-account" ]; then
            distribution_id="$(aws cloudfront list-distributions --output json | python -c 'import json, os, sys; alias = os.environ["CACHE_ALIAS"]; matches = [item["Id"] for item in json.load(sys.stdin).get("DistributionList", {}).get("Items", []) if alias in item.get("Aliases", {}).get("Items", [])]; len(matches) == 1 or sys.exit(f"Expected one CloudFront distribution for {alias}; found {len(matches)}."); print(matches[0])')"
            aws cloudfront create-invalidation --distribution-id "$distribution_id" --paths "/*"
          fi

      - name: Invalidate CloudFront
        if: ${{ inputs.deploy && inputs.invalidate_cache && steps.map.outputs.cache_kind == 'cloudfront' }}
        shell: bash
        env:
          CACHE_VARIABLE: ${{ steps.map.outputs.cache_variable }}
          CACHE_ALIAS: ${{ steps.map.outputs.cache_alias }}
          ASPOSE_COM_QA_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_COM_QA_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_COM_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_COM_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_CLOUD_QA_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_CLOUD_QA_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_CLOUD_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_CLOUD_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_APP_QA_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_APP_QA_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_APP_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_APP_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_AI_QA_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_AI_QA_CLOUDFRONT_DISTRIBUTION_ID }}
          ASPOSE_AI_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.ASPOSE_AI_PRODUCTION_CLOUDFRONT_DISTRIBUTION_ID }}
          CONHOLDATE_CLOUD_QA_CLOUDFRONT_DISTRIBUTION_ID: ${{ vars.CONHOLDATE_CLOUD_QA_CLOUDFRONT_DISTRIBUTION_ID }}
          AWS_ACCESS_KEY_ID: ${{ secrets.HOMEPAGES_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.HOMEPAGES_SECRET_ACCESS }}
          AWS_DEFAULT_REGION: us-east-1
        run: |
          set -euo pipefail
          distribution_id=""
          if [ -n "$CACHE_VARIABLE" ]; then
            distribution_id="${!CACHE_VARIABLE:-}"
          elif [ -n "$CACHE_ALIAS" ]; then
            distribution_id="$(aws cloudfront list-distributions --output json | python -c 'import json, os, sys; alias = os.environ["CACHE_ALIAS"]; matches = [item["Id"] for item in json.load(sys.stdin).get("DistributionList", {}).get("Items", []) if alias in item.get("Aliases", {}).get("Items", [])]; len(matches) == 1 or sys.exit(f"Expected one CloudFront distribution for {alias}; found {len(matches)}."); print(matches[0])')"
          fi
          if [ -z "$distribution_id" ]; then
            echo "::error::CloudFront distribution could not be resolved from variable or alias."
            exit 1
          fi
          if [ -z "${AWS_ACCESS_KEY_ID:-}" ] || [ -z "${AWS_SECRET_ACCESS_KEY:-}" ]; then
            echo "::error::Missing CloudFront invalidation credentials."
            exit 1
          fi
          aws cloudfront create-invalidation --distribution-id "$distribution_id" --paths "/*"

      - name: Purge BunnyCDN
        if: ${{ inputs.deploy && inputs.invalidate_cache && steps.map.outputs.cache_kind == 'bunny' }}
        shell: bash
        env:
          BUNNY_API_KEY: ${{ secrets.BUNNY_API_KEY }}
          CACHE_URL: ${{ steps.map.outputs.cache_url }}
        run: |
          set -euo pipefail
          if [ -z "${BUNNY_API_KEY:-}" ]; then
            echo "::error::Missing BUNNY_API_KEY."
            exit 1
          fi
          if [ -z "${CACHE_URL:-}" ]; then
            echo "::error::Missing Bunny purge URL mapping."
            exit 1
          fi
          curl -fsS -G \
            -H "X-Api-Key: ${BUNNY_API_KEY}" \
            --data-urlencode "url=${CACHE_URL}" \
            "https://api.dynabic.com/bn/purge?async=true" >/dev/null
